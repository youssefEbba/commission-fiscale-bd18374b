# Internationalisation (FR / AR)

Setup i18next + react-i18next, FR par défaut, AR avec RTL natif.

## Architecture

```
src/i18n/
  index.ts              # Init i18next, detector, namespaces
  bootstrap.tsx         # <I18nBootstrap/> : sync <html lang/dir>, persistance
  format.ts             # formatDate, formatDateTime, formatAmount, formatNumber
  enums.ts              # Helpers de traduction des enums métier
  LanguageSwitcher.tsx  # Sélecteur de langue (header)
  locales/
    fr/{common,nav,auth,certificats,utilisations,...}.json
    ar/{common,nav,auth,certificats,utilisations,...}.json
```

Namespaces (27) : `common`, `nav`, `auth`, `landing`, `dashboard`, `notifications`, `validation`, `conventions`, `marches`, `demandes`, `certificats`, `utilisations`, `transferts`, `cloture`, `modifications`, `sous_traitance`, `mise_en_place`, `ged`, `users`, `roles`, `audit`, `referentiel`, `correction_douaniere`, `errors`, `enums`, `reporting`, `simulation`.

## Bilan i18n (post-polish)

- **2777 clés FR / 2777 clés AR** — 0 manquant, 0 orphelin (vérifié par `npm run i18n:check`).
- 27 namespaces ; ~95 % des pages métier migrées (cf. dettes connues plus bas).
- Helpers d'enum centralisés (`tStatutDemande`, `tStatutCertificat`, …) — toutes les anciennes maps `*_STATUT_LABELS` ont été supprimées de `src/lib/api.ts` au profit de tableaux `*_STATUT_VALUES: readonly T[]`.

## Utilisation

```tsx
import { useTranslation } from "react-i18next";

function MyPage() {
  const { t } = useTranslation("certificats");
  return <h1>{t("list.title")}</h1>;
}

// Cross-namespace : préfixer
const { t } = useTranslation(["certificats", "common"]);
t("common:actions.save");
```

### Interpolation

```tsx
t("session.impersonating", { label: "ACME" }); // → « Vous agissez en tant que ACME »
```

### Pluriels (i18next + Intl.PluralRules)

Définir des clés `key_one`, `key_other` (FR) et `key_zero`, `key_one`, `key_two`, `key_few`, `key_many`, `key_other` (AR).

```json
"items_count_one": "{{count}} élément",
"items_count_other": "{{count}} éléments"
```

```tsx
t("items_count", { count: n });
```

## Enums

Les valeurs renvoyées par l'API (`OUVERT`, `DGTCP`, …) restent dans la donnée. Le front les traduit via les helpers de `src/i18n/enums.ts` :

```tsx
import { tStatutCertificat, tRole } from "@/i18n/enums";

<Badge>{tStatutCertificat(cert.statut)}</Badge>
<span>{tRole(user.role)}</span>
```

Ajouter un nouveau mapping : créer la clé dans `enums.json` (`fr` + `ar`), puis exposer un helper dans `enums.ts`.

## Erreurs API

`showApiError(err)` cherche d'abord `errors:<code>` puis retombe sur le message brut du backend.

```ts
import { showApiError } from "@/lib/feedback";
showApiError(err); // titre i18n par défaut
```

## Format dates / montants

```ts
import { formatDate, formatDateTime, formatAmount, formatNumber } from "@/i18n/format";

formatDate("2026-05-18");            // « 18/05/2026 » (fr) — « 18/05/2026 » (ar, chiffres latins)
formatAmount(1500000);               // « 1 500 000 MRU »
formatAmount(1500000, { currency: "USD" });
```

Pour forcer les chiffres arabes : `formatAmount(n, { numberingSystem: "arab" })`.

## RTL

- `<I18nBootstrap/>` met à jour `<html lang|dir>` automatiquement.
- Tailwind v3 fournit les variants `rtl:` et `ltr:` natifs (à utiliser pour les inversions de mise en page).
- **Convention** : préférer les utilitaires logiques (`ms-`/`me-`, `ps-`/`pe-`, `start-`/`end-`, `text-start`/`text-end`) plutôt que `ml-`/`mr-`, etc.
- Icônes directionnelles (chevrons, flèches « → ») : utiliser `rtl:rotate-180` ou un fallback texte.
- Police arabe : `Noto Sans Arabic` chargée via `@fontsource`, appliquée par `html[lang="ar"] body { font-family: ... }`.

## Ajouter une clé

1. Ajouter la clé dans `src/i18n/locales/fr/<ns>.json`.
2. Ajouter la traduction dans `src/i18n/locales/ar/<ns>.json` (même chemin).
3. Utiliser `t("<ns>:<key>")`.
4. Vérifier : `node scripts/i18n-check.mjs`.

## Ajouter une langue

1. Créer `src/i18n/locales/<lang>/` avec les mêmes fichiers que `fr/`.
2. Ajouter le code dans `SUPPORTED_LANGS` (`src/i18n/index.ts`) et importer les JSON.
3. Ajouter l'option dans `LanguageSwitcher.tsx`.
4. Si RTL : adapter `bootstrap.tsx` (déjà géré pour `ar`).

## Outillage & CI

- `npm run i18n:check` (alias `node scripts/i18n-check.mjs`) : compare FR vs AR, code de sortie 1 si AR incomplet.
- En dev, les clés manquantes sont loguées dans la console (`[i18n] Missing key`).
- `saveMissing` est activé uniquement en dev (`import.meta.env.DEV && !import.meta.env.PROD`) — aucun overhead en prod.
- **Pre-commit recommandé** (Husky non installé par défaut) — ajouter manuellement :
  ```bash
  # .husky/pre-commit
  npm run i18n:check
  ```
  À défaut, lancer `npm run i18n:check` avant chaque PR.

## Convention de nommage

- Clés en anglais, `snake_case` ou `kebab-case` selon contexte.
- Hiérarchie : `<page>.<section>.<element>` (ex. `certificats.list.title`).
- Actions communes : `common.actions.*`.
- Statuts : `enums.<enum_name>.<VALEUR_BACKEND>`.
- Erreurs API : `errors.<code_minuscule>` (mappé sur `ApiError.code`).

## Ajouter une valeur d'enum backend

1. Ajouter la valeur dans `src/i18n/locales/fr/enums.json` sous `enums.<group>.<NOUVELLE_VALEUR>`.
2. Idem dans `src/i18n/locales/ar/enums.json`.
3. Si nouveau groupe : créer le helper dans `src/i18n/enums.ts` (`export const tStatutXxx = ...`).
4. Lancer `npm run i18n:check`.

## Dettes connues — non résolues volontairement

| Élément | Localisation | Raison |
|---|---|---|
| `numberToFrenchWords()` | `UtilisationDetail.tsx` | Montant en lettres pour PDF officiel — exigence légale FR uniquement. Commenté `// i18n-intentional`. |
| `DEFAULT_BULLETIN_LIGNES` (libellés FR « Droit de Douane », « TVA ») | `Utilisations.tsx` | Données envoyées au backend si référentiel taxes vide — domaine métier, pas chaîne d'affichage. |
| `entityType` brut JPA (DEMANDE, CERTIFICAT…) | `AuditLogs.tsx` | Pas d'enum backend dédié. Affiché tel quel dans la colonne Entité. |
| Pages non migrées | `ReferentielProjets`, `ReferentielTaxes`, `Roles`, `Utilisateurs`, `Delegues`, `DelegueEditDialog`, `DelegueMarchesDialog`, `Presentation`, `CommissionRelais`, `AssistanceIA`, `ChatbotDGD`, `ExtractionDGD`, `GedConfiguration` | Hors périmètre des lots A→I. Toasts et libellés restent en FR dur. |

## Lexique AR à valider

Aucune clé AR marquée `// REVIEW` à ce stade : le vocabulaire fiscal / douanier (Cordon, AU_CI, A_PAYER, réclamation 48h, DQE, offre fiscale, etc.) a été traduit en s'appuyant sur le vocabulaire administratif standard mauritanien. Une revue terminologique par un expert métier arabophone reste recommandée avant mise en production publique, notamment pour :

- vocabulaire douanier (codes taxes : DD, TVA, RS, AS…),
- statuts spécifiques (`A_RECONTROLER`, `EN_OUVERTURE_DGTCP`, `VALIDE_PRESIDENT`),
- vocabulaire de la correction douanière (réclamation 48h, visa, décision tardive),
- libellés Reporting contextualisés par rôle.

